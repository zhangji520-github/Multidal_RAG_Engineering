LOG_LEVEL: INFO
HOST: 127.0.0.1
PORT: 8001
ORIGINS: ['http://localhost:8080', 'http://127.0.0.1:8080', 'http://192.168.0.136:8080']

# PostgreSQL 数据库配置
# 注意：这里配置了两个独立的数据库
POSTGRES:
  # 用户管理数据库（用于 FastAPI 用户系统）
  USER_DB:
    DRIVER: postgresql
    NAME: user_db
    HOST: localhost
    PORT: 5432
    USERNAME: postgres
    PASSWORD: 200132ji
  
  # LangGraph 数据库（用于对话状态和历史）
  LANGGRAPH_DB:
    DRIVER: postgresql
    NAME: multidal_modal_rag
    HOST: localhost
    PORT: 5432
    USERNAME: postgres
    PASSWORD: 200132ji
    URI: postgresql://postgres:200132ji@localhost:5432/multidal_modal_rag

# Milvus 向量数据库配置
MILVUS:
  URI: http://localhost:19530
  USERNAME: root
  PASSWORD: Milvus
  COLLECTIONS:
    KNOWLEDGE: multimodal_rag          # 知识库集合
    CONTEXT: multimodal_rag_context    # 历史对话上下文集合

# MySQL 数据库配置（如果需要）
MYSQL:
  DRIVER: mysql
  NAME: 
  HOST: 
  PORT: 3306
  USERNAME: root
  PASSWORD: 123123
  QUERY:
    charset: utf8mb4

# JWT 配置
JWT_SECRET_KEY: 09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
ALGORITHM: HS256
ACCESS_TOKEN_EXPIRE_MINUTES: 30

# API 白名单
WHITE_LIST: ['/api/login', '/api/register', '/static', '/docs', '/swagger', '/openapi', '/api/auth']

# 默认密码
DEFAULT_PASSWORD: 123123
